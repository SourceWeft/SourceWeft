# v0.2.0-rc.1 发布准备记录

状态：仅准备，尚未创建或推送标签，尚未触发 Release 工作流。

## 范围和版本

- 版本标签：`v0.2.0-rc.1`，2026-09-14 检查时远端未占用。
- 范围：Web/Docker 自托管候选版，同时准备 macOS Apple Silicon 本地验证包；macOS 分发状态见 `v0.2.0-rc.1-macos.md`，移动端不纳入。
- 已验证的代码基线：`a9e554b01a4ec949d7a03717a1797cb903e7e1b0`。
- 基线 CI：https://github.com/SourceWeft/SourceWeft/actions/runs/34821267634 ，全部 5 个任务成功。
- 本次准备还包含发布说明、部署文档和工作流调整，最终标签应指向这些变更提交后、再次通过 CI 的准确提交，不能直接把上述旧基线当作最终发布提交。
- Web/Docker 以 Git tag 定义版本，不要求修改其他私有工作区包的 `0.0.0`。桌面 package、Tauri、Cargo 及对应锁文件已单独统一为 `0.2.0-rc.1`。

## 预期发布产物

| 产物 | 名称或行为 |
| --- | --- |
| 主镜像标签 | `ghcr.io/sourceweft/sourceweft:v0.2.0-rc.1` |
| SemVer 镜像标签 | `ghcr.io/sourceweft/sourceweft:0.2.0-rc.1` |
| 镜像架构 | `linux/amd64`、`linux/arm64` |
| 自托管附件 | `sourceweft-selfhost-v0.2.0-rc.1.tar.gz` |
| Release 说明 | `releases/v0.2.0-rc.1.md` 加自动生成的变更记录 |
| Release 类型 | prerelease |
| `latest` 与 `0.2` | 不更新 |

本地 `output/release/v0.2.0-rc.1/` 中的压缩包只是按发布命令生成的预览，不是已发布产物，也不证明远端候选镜像已经存在。

## 本次准备检查结果

- `v0.2.0-rc.1` 版本预检通过，结果为 prerelease，`latest=false`。
- 发布配置与自托管配置测试共 9 项通过，Release 工作流 actionlint 与 Git 差异检查通过。
- 安装包预览解压后仅有预期 5 个文件，不包含 `.env` 或真实密钥；本地打包已去除 macOS 的 `._` 元数据文件。
- 使用解压后的模板运行初始化脚本，候选镜像引用、四组独立密钥和 `.env` 的 0600 权限检查通过。
- 解压内容的 `docker compose config --quiet` 通过，未启动服务或拉取候选镜像。
- 已生成并核验 `output/release/v0.2.0-rc.1/SHA256SUMS`。
- 本次准备变更需要新的主 CI 与桌面打包验证结果；基线的绿色 CI 不代表这些新增变更已经通过远端 CI。

## 准备完成后仍需执行

1. 审阅并提交本次准备变更；推送 main 后等待该准确提交的全部 CI 通过。
2. 用户明确授权发布后，再在已验证提交上创建并推送 `v0.2.0-rc.1` 标签。推送该标签会自动触发真实镜像发布。
3. Release 工作流必须完成版本及说明预检、全量 CI、双架构构建和推送、匿名镜像访问检查及自托管附件发布。
4. 检查 GitHub Release 为预发布；两个架构均存在；`latest`/`0.2` 保持不变。
5. 从发布页下载真实附件，以匿名身份拉取完整 RC 标签，按附件说明做一次全新安装与文件/聊天复验。发布失败时先检查已推送的镜像状态，不删除或移动已分发的版本标签来掩盖失败。

## 本次范围以外

- 桌面打包工作流尚无成功运行记录，不能据 Web CI 宣称桌面安装包已可分发。
- 独立 Rust 检查此前的失败发生在移动端图标非 RGBA；同一任务中桌面 Linux 构建和 glib 回归已通过。该错误不属于本次 Web/Docker RC 的质量门禁。
- 保留实测中首条回复及停止状态同步的延迟波动说明，不把功能通过当作性能达标。
