{
  "schemaVersion": 1,
  "id": "sourceweft/video-presentation-tool",
  "kind": "tool",
  "name": "Video Presentation",
  "version": "1.0.0",
  "entry": "./src/index.ts",
  "tools": [
    {
      "id": "load_video_presentation",
      "title": "Load Video Presentation",
      "description": "Load an authorized current video project for an exact edit.",
      "inputSchema": { "type": "object", "additionalProperties": true },
      "outputSchema": { "type": "object" },
      "risk": "write"
    },
    {
      "id": "generate_video_assets",
      "title": "Generate Video Assets",
      "description": "Generate and stage a bounded batch of video visual assets.",
      "inputSchema": { "type": "object", "additionalProperties": true },
      "outputSchema": { "type": "object" },
      "risk": "write"
    },
    {
      "id": "generate_video_narration",
      "title": "Generate Video Narration",
      "description": "Generate, measure, persist, and stage narration tracks.",
      "inputSchema": { "type": "object", "additionalProperties": true },
      "outputSchema": { "type": "object" },
      "risk": "write"
    },
    {
      "id": "validate_video_presentation",
      "title": "Validate Video Presentation",
      "description": "Validate the exact draft and render its required cover and final MP4 in the trusted sandbox.",
      "inputSchema": { "type": "object", "additionalProperties": true },
      "outputSchema": { "type": "object" },
      "risk": "write"
    },
    {
      "id": "publish_video_presentation",
      "title": "Publish Video Presentation",
      "description": "Atomically publish a protected, validated video presentation version.",
      "inputSchema": { "type": "object", "additionalProperties": true },
      "outputSchema": { "type": "object" },
      "risk": "write"
    }
  ]
}
