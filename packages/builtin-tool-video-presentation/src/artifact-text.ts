/**
 * Re-exported from the shared artifact kernel. This module existed verbatim in
 * two capability packages; the implementation now lives in one place.
 */
export { compactArtifactText } from "@sourceweft/contracts/artifact-files";
