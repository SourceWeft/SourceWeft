# domain

Purpose of this directory:

- Store platform-agnostic business rules and use-case logic.
- Keep logic reusable across web, extension, desktop, and backend.

This layer must not depend on browser or desktop runtime APIs.
