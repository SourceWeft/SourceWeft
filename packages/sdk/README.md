# sdk

Purpose of this directory:

- Provide one shared client for backend API calls.
- Centralize auth headers, retries, and response validation.
- Prevent scattered ad hoc `fetch` calls across apps.
