# Mobile shell env (passed through to / used with the web runtime the WebView loads).
# API base URL is configured on apps/web via NEXT_PUBLIC_API_BASE_URL — not here.
NEXT_PUBLIC_WEB_BASE_URL=http://localhost:3000
NEXT_PUBLIC_GOOGLE_MOBILE_CLIENT_ID=
