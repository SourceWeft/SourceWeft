// The working-files module manages thread-scoped virtual filesystem state
// for agent sandbox interactions.  Domain types (WorkingFileRecord,
// WorkingFilePurpose) are defined in the parent content module.
//
// See service.ts for file CRUD operations and paths.ts for path utilities.
