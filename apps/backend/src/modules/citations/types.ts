// The citations module stores LLM-generated citation references that link
// assistant message spans back to source chunks, documents, and external URIs.
// It does not re-export its internal parameter types — the public surface is
// the repository functions in repository.ts.
