# content module

MVP domain write-paths for workspace-scoped content:

- source creation and indexing
- thread creation
- thread stream endpoint (non-SSE JSON response in MVP)

Billing metering hooks are integrated in indexing and stream flows.
