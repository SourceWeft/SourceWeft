/**
 * One set of limits for ingest AND for running: defined in
 * `@sourceweft/skill-format` so the CLI holds a downloaded skill to the same
 * bounds the registry indexed it under.
 */
export { SKILL_STORAGE_LIMITS } from "@sourceweft/skill-format";
