"use client";

import type { DashboardSkillSlotProps } from "./slot-props";

/** The AI overview at the top of the Overview tab (§17.4). */
export function SkillAiOverview(props: DashboardSkillSlotProps) {
  void props;
  return null;
}
