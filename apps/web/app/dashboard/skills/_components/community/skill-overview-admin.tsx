"use client";

import type { DashboardSkillSlotProps } from "./slot-props";

/** Market admin controls for the AI overview (§17.4); renders nothing for anyone else. */
export function SkillOverviewAdmin(_props: DashboardSkillSlotProps) {
  return null;
}
