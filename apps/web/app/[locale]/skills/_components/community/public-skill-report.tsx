import type { PublicSkillSlotProps } from "./slot-props";

/** The report link in the attribution section (§17.2). */
export async function PublicSkillReport(_props: PublicSkillSlotProps) {
  return null;
}
