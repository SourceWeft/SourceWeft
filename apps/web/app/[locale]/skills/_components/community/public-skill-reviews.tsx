import type { PublicSkillSlotProps } from "./slot-props";

/** Ratings and reviews below the page body (§17.3). */
export async function PublicSkillReviews(_props: PublicSkillSlotProps) {
  return null;
}
