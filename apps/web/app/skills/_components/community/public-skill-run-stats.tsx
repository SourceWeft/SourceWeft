import type { PublicSkillSlotProps } from "./slot-props";

/** Sandbox run stats in the side column (§17.5). */
export async function PublicSkillRunStats(_props: PublicSkillSlotProps) {
  return null;
}
